<UserControl x:Class="FrameSel.UCFrameSel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FrameSel"
             mc:Ignorable="d" 
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             d:DesignHeight="300" d:DesignWidth="200" Margin="{Binding Margin}">
    <Grid>
        <Label Content="Frame" FontSize="20" Margin="109,73,91,187" Width="100" Height="40"/>
        <ComboBox ItemsSource="{Binding m_frameList}"  Width="120" Height="55" HorizontalContentAlignment="Center" FontSize="30">
            <b:Interaction.Triggers>
                <b:EventTrigger EventName="SelectionChanged">
                    <b:InvokeCommandAction Command="{Binding SelectChagne}"
                                           PassEventArgsToCommand="True" />
                </b:EventTrigger>
            </b:Interaction.Triggers>
        </ComboBox>
    </Grid>
</UserControl>
